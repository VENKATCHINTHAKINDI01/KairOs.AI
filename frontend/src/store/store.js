/**
 * KairosAI — Central Store
 * Exports all state stores for use across components.
 * Uses lightweight vanilla JS stores (no Redux/Zustand dependency).
 */

export { warRoomStore } from './warRoomSlice'
export { agentStore }   from './agentSlice'